spring:
  application:
    name: authenticationservice
  profiles:
    active:
      default
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        service-id: configserver